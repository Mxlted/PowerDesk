<UserControl x:Class="PowerDesk.Modules.HashDesk.Views.HashDeskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SurfaceBaseBrush}"
             Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="32,24" AllowDrop="True" Drop="Root_Drop" DragOver="Root_DragOver">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Style="{StaticResource TextDisplay}" Text="HashDesk"/>
                <TextBlock Style="{StaticResource TextCaption}" Margin="0,4,0,0"
                           Text="Compute file and text checksums locally."/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Clear" Command="{Binding ClearResultsCommand}"/>
                <Button Content="Add files" Margin="8,0,0,0" Style="{StaticResource PrimaryButton}" Command="{Binding SelectFilesCommand}"/>
            </StackPanel>
        </DockPanel>

        <WrapPanel Grid.Row="1" Margin="0,16,0,16">
            <Border Style="{StaticResource Card}" Width="180" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Results" Style="{StaticResource TextCaption}"/>
                    <TextBlock Text="{Binding ResultCount}" Style="{StaticResource TextTitle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Width="320" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Selected SHA256" Style="{StaticResource TextCaption}"/>
                    <TextBlock Text="{Binding SelectedResult.Sha256, FallbackValue=-}" FontFamily="Consolas"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <TabControl Grid.Row="2">
            <TabItem Header="Files">
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="380"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                        <DataGrid ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedResult}" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="*"/>
                                <DataGridTextColumn Header="Size" Binding="{Binding SizeLabel}" Width="90"/>
                                <DataGridTextColumn Header="SHA256" Binding="{Binding Sha256}" Width="260"/>
                                <DataGridTextColumn Header="Done" Binding="{Binding CompletedLabel}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Border Grid.Column="1" Margin="16,0,0,0">
                        <Grid>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Visibility="{Binding SelectedResult, Converter={StaticResource NullToVis}, ConverterParameter=invert}">
                                <TextBlock Text="Drop files here" Style="{StaticResource TextSubtitle}" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>

                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                          Visibility="{Binding SelectedResult, Converter={StaticResource NullToVis}}">
                                <StackPanel DataContext="{Binding SelectedResult}">
                                    <TextBlock Text="{Binding FileName}" Style="{StaticResource TextSubtitle}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding FilePath}" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="12"/>
                                    <Border Background="{DynamicResource DividerBrush}" Height="1" Margin="0,12"/>
                                    <TextBlock Text="SHA256" Style="{StaticResource TextCaption}"/>
                                    <TextBox Text="{Binding Sha256}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap"/>
                                    <TextBlock Text="SHA1" Style="{StaticResource TextCaption}" Margin="0,10,0,0"/>
                                    <TextBox Text="{Binding Sha1}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap"/>
                                    <TextBlock Text="MD5" Style="{StaticResource TextCaption}" Margin="0,10,0,0"/>
                                    <TextBox Text="{Binding Md5}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap"/>
                                    <Button Content="Copy SHA256" Margin="0,12,0,0"
                                            Command="{Binding DataContext.CopySha256Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Text">
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="420"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBox Grid.Row="0" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto" MinHeight="260"/>
                            <Button Grid.Row="1" Content="Compute text hashes" Style="{StaticResource PrimaryButton}"
                                    HorizontalAlignment="Right" Margin="0,12,0,0"
                                    Command="{Binding ComputeTextHashCommand}"/>
                        </Grid>
                    </Border>
                    <Border Grid.Column="1" Margin="16,0,0,0">
                        <StackPanel>
                            <TextBlock Text="SHA256" Style="{StaticResource TextCaption}"/>
                            <TextBox Text="{Binding TextSha256}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap"/>
                            <TextBlock Text="SHA1" Style="{StaticResource TextCaption}" Margin="0,10,0,0"/>
                            <TextBox Text="{Binding TextSha1}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap"/>
                            <TextBlock Text="MD5" Style="{StaticResource TextCaption}" Margin="0,10,0,0"/>
                            <TextBox Text="{Binding TextMd5}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap"/>
                            <Button Content="Copy SHA256" Margin="0,12,0,0" Command="{Binding CopyTextSha256Command}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
